spec:
  name: stengttunnel-api
  services:
    - dockerfile_path: Dockerfile
      git:
        branch: main
        repo_clone_url: https://github.com/olekenneth/stengttunnel-api.git
        name: stengttunnel-api
